name: gpt-oss-scoring
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - vllm==0.10.1.1
      - openai-harmony
      - transformers==4.56.1
      - huggingface_hub>=0.34.0,<1.0
      - torch
